# Machine-local profile values — COPY this to profile.local.ps1 (gitignored)
# and fill in. Dot-sourced by personal.ps1 / work.ps1.
# Keep any secrets ONLY in the copy, never here.
#
# Git name/email are NOT set here — they come from your global gitconfig
# (~/.gitconfig). Only override them below if a specific machine needs to.

#-----MACHINE-PRIVATE SETTINGS-----#
# e.g. $env:HTTP_PROXY = '...'  /  API tokens  /  machine-specific PATH entries.
